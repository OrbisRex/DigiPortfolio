{% extends 'base.html.twig' %}

{% block title %}DigiPortfolio | File Management{% endblock %}

{% block body %}
    <section id="top" class="main">
        <h1>Files</h1>
        <div class="wide-white-block">
            <div class="button-list">
            {%if files %}
                {% for file in files %}
                    <span class="button"><a href="{{ asset(file.path) }}" title="Open file in new tab." target="_blank">{{ file.name }} (^</a></span>
                {% endfor %}
            {% else %}
                <p class='info'>No file found. Add some!</p>
            {% endif %}
            </div>
        </div>    
    </section>

    <section id="new" class="lightgray">
        <h2>New files</h2>
        <div class="wide-envelop">
            {% if error != FALSE %}
                <p class="error">{{ error }}</p>
            {% endif %}

            {{ form_start(form) }}
                {{ form_errors(form) }}
                {{ form_row(form.files) }}

                <div class="wide">
                    {{ form_row(form.save) }}
                </div>
            {{ form_end(form) }}
        </div>
    </section>

    <section id="image" class="darkblue">
        <h2>Images</h2>
        <div class="wide-envelop">
            <div class="button-list">
            {%if images %}
                {% for image in images %}
                    <span class="button"><a href="{{ asset(image.path) }}" title="Open file in new tab." target="_blank">{{ image.id }}] {{ image.name }} (^</a></span>
                {% endfor %}
            {% else %}
                <p class='info'>No file found. Add some!</p>
            {% endif %}
            </div>
        </div>
    </section>

    <section id="other" class="lightgray">
        <h2>CSV files</h2>
        <div class="wide-envelop">
            <div class="button-list">
            {%if csvFiles %}
                {% for file in csvFiles %}
                    <span class="button"><a href="{{ asset(file.path) }}" title="Download {{ file.type }}">{{ file.id }}] {{ file.name }} (v</a></span>
                {% endfor %}
            {% else %}
                <p class='info'>No file found. Add some!</p>
            {% endif %}
            </div>
        </div>
    </section>

{% include "file/navigation.html.twig" %}
{% endblock %}
